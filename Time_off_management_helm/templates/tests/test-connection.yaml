apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evvo.fullname" . }}-test-connection"
  labels:
    {{- include "evvo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evvo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
